# TODO — план развития NotepadM

Порядок работы: сверху вниз. Выполненный пункт помечается `[x]` со ссылкой
на запись в [dev-log.md](dev-log.md); детали реализации — там.

## 1. Фундамент

- [x] **Тесты** — [запись 9](dev-log.md)
  - [x] юнит-тесты `find_matches`: несколько вхождений, учёт/игнор регистра,
        кириллица, пустой запрос, корректность байтовых границ
  - [x] тесты определения кодировки
  - [x] шаг `cargo test` в CI
- [x] **Кодировки** — [запись 9](dev-log.md)
  - [x] чтение не-UTF-8 файлов (Windows-1251 и др.): `encoding_rs` + `chardetng`
  - [x] хранить кодировку в `Document`, сохранять файл в исходной кодировке
  - [ ] показывать кодировку (появится вместе со строкой состояния)
- [x] **Документация репозитория** — [запись 9](dev-log.md)
  - [x] переписать корневой `README.md` (сейчас описывает Java-версию)
  - [x] создать `CLAUDE.md` (команды, архитектура, соглашения проекта)

## 2. Надёжность

- [x] повторное открытие уже открытого файла → переключение на его вкладку,
      а не дубликат — [запись 12](dev-log.md)
- [x] путь к файлу аргументом командной строки («Открыть с помощью…»
      из проводника) — [запись 12](dev-log.md)
- [x] drag-and-drop файла в окно — [запись 12](dev-log.md)
- [x] панель вкладок при большом числе вкладок: сжатие заголовков —
      [запись 10](dev-log.md)

## 3. Функции

- [ ] строка состояния: позиция курсора (строка:столбец), кодировка, размер текста
- [ ] недавние файлы в меню «Файл» (потянет хранение состояния между
      запусками — крейт `dirs`, файл настроек)
- [ ] настройки: шрифт и размер, с сохранением между запусками
- [ ] зум: Ctrl+колесо / Ctrl+«+»/«−»
- [ ] переключатель переноса строк (меню «Вид»)
- [x] иконка у `notepadm.exe` (крейт `winresource` + NotepadM.ICO из истории git) —
      [запись 11](dev-log.md)
- [ ] свой набор иконок в едином стиле (текущие 16px мелковаты на HiDPI)

## 4. Большие этапы

- [ ] собственный виджет редактора: подсветка синтаксиса (HTML и др.),
      подсветка **всех** вхождений поиска, виртуализация для больших файлов
      (потолок производительности на больших файлах — сам TextEdit,
      см. раздел 5)
- [ ] глубокий undo/redo со своей историей (сейчас — встроенный Ctrl+Z редактора)
- [ ] печать (в Java-версии была иконка Print; в Slint печати нет — исследовать)

## 5. Оптимизация и гигиена кода

По итогам ревизии проекта (2026-07-15), в порядке отдачи:

- [ ] `[profile.release]` в Cargo.toml: `lto = true`, `codegen-units = 1`,
      `strip = true` — минус 30–50 % от 15,8 МБ exe, быстрый выигрыш
- [x] не копировать весь текст на каждое нажатие: ленивая синхронизация
      (`sync_active_doc` в точках синхронизации; при вводе — только dirty) —
      [запись 14](dev-log.md)
- [ ] кеш результатов поиска в `SearchState`: `find_matches` пересчитывает
      все вхождения на каждый F3 (строчная копия + карта байтов заново);
      делать вместе с подсветкой всех вхождений из раздела 4
- [ ] урезать фичи Slint: в сборке два рендерера (femtovg + software),
      используется один — подобрать список фич, проверить CI на трёх ОС
- [ ] общая функция для фильтров файловых диалогов («Открыть»/«Сохранить»
      дублируют список расширений)
- [ ] сбрасывать состояние поиска при переключении вкладки через `open_path`
      (кликом по вкладке — сбрасывается, повторным открытием файла — нет)

## Отложено (ждёт отдельного решения)

- [ ] `git push -u origin rust` + первый прогон CI на трёх ОС
